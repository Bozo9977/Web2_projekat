import { FlightQuickReservation } from './flight-quick-reservation';

describe('FlightQuickReservation', () => {
  it('should create an instance', () => {
    expect(new FlightQuickReservation()).toBeTruthy();
  });
});
