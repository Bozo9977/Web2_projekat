import { MakeFlightQuickReservations } from './make-flight-quick-reservations';

describe('MakeFlightQuickReservations', () => {
  it('should create an instance', () => {
    expect(new MakeFlightQuickReservations()).toBeTruthy();
  });
});
